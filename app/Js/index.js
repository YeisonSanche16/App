const hello = (name) => {
 return `hello ${name}`;
};
let odds = evens.map((v) => v + 1);
let nums = evens.map((v, i) => v + i);
setTimeout(() => {
console.log('ES2015 FTW');
}, 1000);
console.log('Hola');
