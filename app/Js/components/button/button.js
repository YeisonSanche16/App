const hello = (name) => {
  return `hello ${name}`;
};
const odds = evens.map((v) => v + 1);
const nums = evens.map((v, i) => v + i);
setTimeout(() => {
  console.log('ES2015 FTW');
}, 1000);
console.log('Hola');
